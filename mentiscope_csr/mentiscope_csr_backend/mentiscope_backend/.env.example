# Database connection for the shared MentiScope platform schema.
# For local development, omit this and SQLite (./mentiscope.db) will be used automatically.
# For PostgreSQL (per platform standard), set:
DATABASE_URL=postgresql+psycopg2://mentiscope:changeme@localhost:5432/mentiscope
